apiVersion: apps/v1
kind: Deployment
metadata:
  name: elasticsearch-guide
  namespace: elasticsearch-guide
spec:
  replicas: 2
  selector:
    matchLabels:
      app: elasticsearch-guide
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: elasticsearch-guide
    spec:
      containers:
        - name: elasticsearch-guide
          image: itsanla/elasticsearch-s6:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: PORT
              value: "8080"
          resources:
            requests:
              memory: "16Mi"
              cpu: "25m"
            limits:
              memory: "64Mi"
              cpu: "100m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: elasticsearch-guide-service
  namespace: elasticsearch-guide
spec:
  selector:
    app: elasticsearch-guide
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
